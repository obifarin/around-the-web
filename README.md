
  # Around The Web

  This is a code bundle for Around The Web. 
  
  ## Running the code
  
  Run `npm i` to install the dependencies.
  
  Run `npm run dev` to start the development server.
  
  This is a single-page Vite + React app and can be deployed to Vercel by running `npm run build` and serving the `dist` output.
  